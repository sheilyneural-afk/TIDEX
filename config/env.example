# Example operator flags. Not loaded by the Rust crate.
# Runtime paths come from ./tidex (TIDEX_HOME, TIDEX_PRIVATE_ROOT, TIDEX_HF_PYTHON).
# Copy to .env.local if a wrapper needs them; .env* is gitignored.

ENVIRONMENT=development
LOG_LEVEL=debug
