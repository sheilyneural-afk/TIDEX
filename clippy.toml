# Clippy configuration values only. Lint levels belong in Cargo.toml or the
# command line; putting warn/allow/deny arrays here makes Clippy reject the file.
allow-unwrap-in-tests = true
allow-expect-in-tests = true
allow-panic-in-tests = true
