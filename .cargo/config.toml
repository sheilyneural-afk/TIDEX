[net]
offline = false

[build]
incremental = false
# Mantener los artefactos de Cargo fuera del checkout y fuera del runtime
# operativo. La ruta es relativa al checkout, no a una cuenta de usuario fija.
target-dir = "../.cache/tidex/cargo-target"
